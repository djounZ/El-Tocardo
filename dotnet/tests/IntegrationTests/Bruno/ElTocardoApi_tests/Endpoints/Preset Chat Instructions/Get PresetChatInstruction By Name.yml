info:
  name: Get PresetChatInstruction By Name
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_backend}}/v1/preset-chat-instructions/example-instruction"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
