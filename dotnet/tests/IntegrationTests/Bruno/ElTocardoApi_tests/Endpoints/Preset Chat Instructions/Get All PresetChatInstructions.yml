info:
  name: Get All PresetChatInstructions
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_backend}}/v1/preset-chat-instructions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
