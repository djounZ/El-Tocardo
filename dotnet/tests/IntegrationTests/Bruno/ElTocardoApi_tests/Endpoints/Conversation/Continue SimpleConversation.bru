meta {
  name: Continue SimpleConversation
  type: http
  seq: 2
}

post {
  url: {{api_backend}}/v1/conversation/continue
  body: json
  auth: inherit
}

body:json {
  {
    "conversation_id": "{{simple_conversation_id}}",
    "input_message": {
      "role": "user",
      "contents": [
        {
          "$type": "text",
          "text": "What are your skills ? how can you help me ?"
        }
      ]
    }
  }
}

settings {
  encodeUrl: true
}
