info:
  name: Delete MCP server
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{api_backend}}/v1/mcp-servers/desktop-commander"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
