info:
  name: Get MCP server by name
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_backend}}/v1/mcp-servers/fetch"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
