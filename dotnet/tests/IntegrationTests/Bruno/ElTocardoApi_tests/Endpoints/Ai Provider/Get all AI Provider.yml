info:
  name: Get all AI Provider
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_backend}}/v1/ai-providers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
