info:
  name: Get AI Provider
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_backend}}/v1/ai-providers/github_copilot"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
