info:
  name: Delete preset chat options
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{api_backend}}/v1/preset-chat-options/presetChatOptionsTest"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
