info:
  name: Get all preset chat options
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_backend}}/v1/preset-chat-options"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
