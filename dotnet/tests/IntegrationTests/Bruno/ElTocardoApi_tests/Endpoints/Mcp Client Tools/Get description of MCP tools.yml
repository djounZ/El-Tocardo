info:
  name: Get description of MCP tools
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_backend}}/api/mcp_tools"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
