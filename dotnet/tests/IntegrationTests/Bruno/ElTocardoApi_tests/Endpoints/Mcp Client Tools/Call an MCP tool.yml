info:
  name: Call an MCP tool
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_backend}}/api/mcp_tools/call"
  body:
    type: json
    data: |-
      {
        "server_name": "fetch",
        "tool_name": "fetch",
        "arguments": {
          "url": "https://fr.wikipedia.org/wiki/Grand_mod%C3%A8le_de_langage"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
