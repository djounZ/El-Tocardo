info:
  name: connect/userinfo
  type: http
  seq: 4

http:
  method: GET
  url: "{{authorization_server}}/connect/userinfo"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
