info:
  name: testconfiguration
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_backend}}/configuration"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
